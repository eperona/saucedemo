{
  "name": "saucedemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --project=sanity --project=regression",
    "test:allure": "npx playwright test --reporter=allure-playwright",
    "test:allure:report:ci": "npx allure generate --clean && npx allure open --port 8080",
    "runAllSanityTests": "npx playwright test --project=sanity",
    "runAllRegressionTests": "npx playwright test --project=sanity --project=regression"
  },
  "keywords": [],
  "author": "Edwin Perona",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.0.14",
    "allure-commandline": "^2.34.1"
  },
  "dependencies": {
    "allure-playwright": "^2.0.0",
    "dotenv": "^17.2.0"
  }
}
